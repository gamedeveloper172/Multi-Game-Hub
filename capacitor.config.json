const config = {
  appId: "com.maxapps.multigamehub",
  appName: "MultiGameHub",
  webDir: ".",     // Change to "build" or your Vercel output directory
  bundledWebRuntime: false
};

export default config;
